#!/usr/bin/env sh

if gem list --no-installed --exact --silent foreman; then
  echo "Installing foreman..."
  gem install foreman
fi

# Use port 2030 as specified in .env.development
PORT=2030 exec foreman start -f Procfile.dev "$@"
